interface IPlayer {
    name: string;
    teamName: string;
    position: string;
    starting: boolean;
    injuredReserve: boolean;
    // name, team name, position, starting?
    // injured?
}

export default IPlayer;